# Both credentials sourced from Secrets the user manages.
# Expect: no chart-managed Secret is rendered.
schematic:
  existingSecret: schematic-api
  existingSecretKey: api-key
redis:
  addr: redis:6379
  existingSecret: redis-auth
  existingSecretKey: password
